# SSH tunnel config for USB Beamer Server (secondary SSH daemon on port 8007)

Port 8007
Protocol 2
HostKey /opt/beamer/ssh_host_rsa_key
HostKey /opt/beamer/ssh_host_ed25519_key

PermitRootLogin prohibit-password
PasswordAuthentication no
ChallengeResponseAuthentication no

# Allow tunneling but prevent terminal/shell access
AllowTcpForwarding yes
PermitTTY no
X11Forwarding no
AllowAgentForwarding no
# Restrict TCP forwarding to usbipd (3240) and the local USB API (6000)
PermitOpen localhost:3240 localhost:6000
ForceCommand /usr/bin/tail -f /dev/null

UsePAM no
PrintMotd no